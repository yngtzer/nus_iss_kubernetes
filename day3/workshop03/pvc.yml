
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bggdb-pvc
  labels:
    name: bggdb-pvc
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: do-block-storage